<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/editTextStudentName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Student Name"
        android:inputType="text"/>

    <EditText
        android:id="@+id/editTextCMSId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextStudentName"
        android:layout_marginTop="16dp"
        android:hint="CMS ID"
        android:inputType="text"/>

    <Button
        android:id="@+id/btnAddStudent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextCMSId"
        android:layout_marginTop="16dp"
        android:text="Add Student"
        android:onClick="addStudent"/>

    <Button
        android:id="@+id/btnDeleteStudent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnAddStudent"
        android:layout_marginTop="16dp"
        android:text="Delete Student"
        android:onClick="deleteStudent"/>

    <Button
        android:id="@+id/btnViewAllStudents"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnDeleteStudent"
        android:layout_marginTop="16dp"
        android:text="View All Students"
        android:onClick="viewAllStudents"/>

    <TextView
        android:id="@+id/textViewStudentDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnViewAllStudents"
        android:layout_marginTop="16dp"
        android:text=""
        android:textSize="18sp"/>
</RelativeLayout>
